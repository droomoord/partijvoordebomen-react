import React from 'react';
import classes from './Header.module.css';

const header = props => {
  return <h1 className={classes.header}>Partij voor de bomen op Texel</h1>;
};

export default header;
